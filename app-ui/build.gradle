plugins {
    id 'java'
    id "com.moowork.node" version "1.2.0"
}

node {
    version = '9.2.0'
    download = true
}

jar.dependsOn 'yarn_install'
jar.dependsOn 'yarn_run_build'

jar {
    from 'dist/app-ui' into 'static'
}
